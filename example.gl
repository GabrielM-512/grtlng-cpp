i32 main() {
    for (u16 i = 1; i < 30; i++) {
        i64 a = fib(i);
        print a;
    }

    u32 i = 0;

    while (i <= fib(15)) {
        print i / fib(15);
        i++;
    }

}

i64 fib(u64 num) {
    if (num < 2) return num;

    return fib(num - 1) + fib(num - 2);
}